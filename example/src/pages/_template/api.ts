import {
  actions as apiExampleActions,
  selectors as apiExampleSelectors,
  reducers as apiExampleReducers,
} from 'app-src/api/_template'

export {
  apiExampleActions,
  apiExampleSelectors,
  apiExampleReducers,
}
